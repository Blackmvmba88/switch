# Semantic Bus

The heart of BCR. It transforms raw inputs (e.g., `B1`, `A9`) into semantic controls (e.g., `A`, `DPad_Up`).
- **Profile:** `profiles/*.normalized.json`.
- **Logic:** `runtime/translate-sample.js`.
