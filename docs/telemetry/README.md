# Telemetry & Observability

BCR provides deep visibility into its internal state.
- **Metrics:** Latency, jitter, packet loss (`bmctl net-status`).
- **Logs:** Live event stream (`live-events.jsonl`).
- **Reports:** `reports/live-status.json`.
